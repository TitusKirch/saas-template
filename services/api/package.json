{
  "name": "@tituskirch/api",
  "private": true,
  "scripts": {
    "dev": "php artisan pulse:check & php artisan pulse:work & php artisan schedule:work & php artisan horizon & php artisan serve",
    "lint": "./vendor/bin/pint --test",
    "lint:fix": "./vendor/bin/pint",
    "test": "php artisan test --stop-on-failure"
  },
  "type": "module",
  "devDependencies": {
    "axios": "^1.7.2",
    "laravel-vite-plugin": "^1.0.4",
    "vite": "^5.2.13"
  }
}

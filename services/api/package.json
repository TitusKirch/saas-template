{
  "name": "@tituskirch/api",
  "private": true,
  "scripts": {
    "dev": "php artisan pulse:check & php artisan pulse:work & php artisan schedule:work & php artisan queue:work & php artisan serve",
    "lint": "./vendor/bin/pint --test",
    "lint:fix": "./vendor/bin/pint",
    "test": "php artisan test --stop-on-failure"
  },
  "type": "module",
  "devDependencies": {
    "axios": "^1.6.8",
    "laravel-vite-plugin": "^1.0.2",
    "vite": "^5.2.11"
  }
}
